server:
  port: 8080
spring:
  application:
    name: zodiac-service
  cloud:
    consul:
      config:
        enabled: true
      discovery:
        instanceId: ${spring.application.name}:${random.value}